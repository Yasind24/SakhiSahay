[build]
  command = "corepack pnpm install --frozen-lockfile && corepack pnpm --filter @workspace/sakhi-sahay run build"
  publish = "artifacts/sakhi-sahay/dist/public"

[build.environment]
  NODE_VERSION = "22"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
